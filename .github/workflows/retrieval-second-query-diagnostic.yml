name: Retrieval Second Query Diagnostic

on:
  push:
    branches:
      - retrieval/second-query-strategy-diagnostic
    paths:
      - src/track_only_replay_diagnostic.py
      - .github/workflows/retrieval-second-query-diagnostic.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Download saved benchmark artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p prior-retrieval prior-replay
          gh run download 34570058054 \
            --repo "${{ github.repository }}" \
            --name spotify-retrieval-benchmark \
            --dir prior-retrieval
          gh run download 34753411423 \
            --repo "${{ github.repository }}" \
            --name matcher-replay \
            --dir prior-replay
          test -f prior-retrieval/retrieval_benchmark.json
          test -f prior-replay/matcher_replay.json

      - name: Replay saved track-only candidates as query two
        run: >-
          python -m src.track_only_replay_diagnostic
          prior-retrieval/retrieval_benchmark.json
          prior-replay/matcher_replay.json
          data/tunemymusic_missing_7.txt
          --json track_only_replay_diagnostic.json

      - name: Upload diagnostic report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: track-only-replay-diagnostic
          path: track_only_replay_diagnostic.json
          if-no-files-found: error
